name: f1_analytics
version: "1.0.0"
profile: f1_analytics

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  f1_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
